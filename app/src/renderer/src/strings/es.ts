/**
 * Spanish (es-ES) localization strings.
 *
 * This is a stub for translators. All strings are empty and must be
 * translated by a native Spanish speaker. TypeScript will enforce that
 * the shape matches the English source (strings/en.ts).
 */

import type { Strings } from './en'

export const strings: Strings = {
  practice: {
    title: '' as any,
    subtitle: '' as any,
    fieldFullName: '' as any,
    fieldCredentials: '' as any,
  },
  clinical: {
    title: '' as any,
    subtitle: '' as any,
    evalTypes: '' as any,
    instruments: '' as any,
  },
} as const
